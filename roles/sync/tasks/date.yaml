---

- name: get Ansible host date
  local_action:
    module: command
    _raw_params: "date"
  register: host_date
  failed_when: no
  changed_when: no
  tags:
  - date

- name: set Ansible host date to remote
  shell: date -s "{{ host_date.stdout }}"
  become: true
  tags:
  - date

