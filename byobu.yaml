# To install and use: `ansible-galaxy install thomfab.byobu`

- hosts:
    - all
  become: true
  roles:
    - thomfab.byobu
  vars:
    # Ensure byobu is by default enabled
    byobu_launch_by_default: True
